package com.proyectofinal.ui.utils

import androidx.compose.material3.windowsizeclass.WindowWidthSizeClass

enum class ContentType {
    LIST_ONLY,
    LIST_AND_DETAIL
}

enum class NavigationType {
    BOTTOM_NAVIGATION,
    NAVIGATION_RAIL,
    PERMANENT_NAVIGATION_DRAWER
}